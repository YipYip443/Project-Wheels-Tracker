import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    titleView: {
        marginTop: '30%',
        width: '100%',
        alignItems: 'center',
        flex: 1,
    }
});

export default styles;